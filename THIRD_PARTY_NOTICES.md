# Third-Party Notices

Blackwall Core 0.1 has no runtime package dependencies and contains no third-party binaries, antivirus signatures, model weights, or community detection rules.

Development automation uses GitHub-maintained Actions as declared under `.github/workflows/`. Those services and actions are not distributed as part of the package.

Update this file whenever a distributed dependency is added.
